// SlideEngine removed in Tiny Slider port rewrite.
// Index management lives inside Slider._updateIndex().
import { describe, it } from 'vitest'
describe('SlideEngine (removed)', () => {
  it('placeholder — index logic moved into Slider', () => {})
})
