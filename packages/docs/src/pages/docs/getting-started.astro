---
import DocsLayout from '../../layouts/DocsLayout.astro'
import SponsorBanner from '../../components/SponsorBanner.astro'
---
<DocsLayout title="Getting Started">
  <h1>Getting Started</h1>
  <p class="lead">ACSlider is a modular, accessible slider library written in TypeScript. The core is always required; plugins, effects, and WebGL are optional.</p>

  <h2>Installation</h2>
  <p>Install with your preferred package manager:</p>
  <pre><code>pnpm add @acslider/core
pnpm add @acslider/plugins    # optional
pnpm add @acslider/effects    # optional
pnpm add @acslider/webgl      # optional</code></pre>

  <pre><code>npm install @acslider/core @acslider/plugins
yarn add @acslider/core @acslider/plugins</code></pre>

  <h2>HTML structure</h2>
  <p>ACSlider expects a container element with a wrapper and slides inside:</p>
  <pre><code>{`<div class="c--slider-a" id="my-slider">
  <div class="c--slider-a__wrapper">
    <div class="c--slider-a__slide">Slide 1</div>
    <div class="c--slider-a__slide">Slide 2</div>
    <div class="c--slider-a__slide">Slide 3</div>
  </div>
</div>`}</code></pre>

  <p>Alternatively, you can use the <code>data-slide</code> attribute instead of the class name to mark slides.</p>

  <h2>Basic usage</h2>
  <pre><code>{`import { Slider } from '@acslider/core'

const slider = new Slider('#my-slider', {
  slidesPerPage: 1,
  loop: true,
  speed: 400,
})`}</code></pre>

  <h2>With plugins</h2>
  <pre><code>{`import { Slider } from '@acslider/core'
import { arrows, pagination, autoplay } from '@acslider/plugins'

const slider = new Slider('#my-slider', {
  slidesPerPage: 1,
  loop: true,
  speed: 400,
  plugins: [
    arrows(),
    pagination({ type: 'dots', clickable: true }),
    autoplay({ delay: 3000, pauseOnHover: true }),
  ],
})`}</code></pre>

  <h2>Styling</h2>
  <p>Import the SCSS from <code>@acslider/core</code>:</p>
  <pre><code>@use '@acslider/core/scss/slider';</code></pre>
  <p>Or import the CSS directly if you don't use SCSS:</p>
  <pre><code>import '@acslider/core/dist/slider.css'</code></pre>
  <p>Variables can be overridden before the import:</p>
  <pre><code>{`$slider-bg: transparent;
$slider-width: 100%;
@use '@acslider/core/scss/slider';`}</code></pre>

  <h2>Next steps</h2>
  <ul>
    <li><a href="/docs/options">All options</a></li>
    <li><a href="/docs/methods">Methods reference</a></li>
    <li><a href="/docs/events">Events reference</a></li>
    <li><a href="/docs/plugins">Plugins overview</a></li>
    <li><a href="/demos/base">Browse demos</a></li>
  </ul>

  <SponsorBanner />
</DocsLayout>
