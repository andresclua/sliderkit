---
import DocsLayout from '../../layouts/DocsLayout.astro'
---
<DocsLayout title="Styling">
  <h1>Styling</h1>
  <p class="lead">SliderKit uses a BEM-based CSS class system. All classes are prefixed with <code>c--slider-a</code>.</p>

  <h2>HTML structure</h2>
  <pre><code>{`<div class="c--slider-a" id="my-slider">
  <div class="c--slider-a__wrapper">
    <div class="c--slider-a__item" data-slide>Slide 1</div>
    <div class="c--slider-a__item" data-slide>Slide 2</div>
  </div>
</div>`}</code></pre>

  <h2>Core CSS classes</h2>
  <table>
    <thead><tr><th>Class</th><th>Applied to</th><th>Purpose</th></tr></thead>
    <tbody>
      <tr><td><code>.c--slider-a</code></td><td>Container</td><td>Overflow hidden, position relative, sets the viewport</td></tr>
      <tr><td><code>.c--slider-a__wrapper</code></td><td>Wrapper</td><td>Flex row (or column in vertical mode); translate animates here</td></tr>
      <tr><td><code>.c--slider-a__item</code></td><td>Each slide</td><td>Flex shrink 0; width set by <code>applyLayout()</code></td></tr>
    </tbody>
  </table>

  <h2>State classes</h2>
  <table>
    <thead><tr><th>Class</th><th>Applied to</th><th>When</th></tr></thead>
    <tbody>
      <tr><td><code>.c--slider-a__item--active</code></td><td>Item</td><td>The current visible slide</td></tr>
      <tr><td><code>.c--slider-a__item--prev</code></td><td>Item</td><td>Slide immediately before active</td></tr>
      <tr><td><code>.c--slider-a__item--next</code></td><td>Item</td><td>Slide immediately after active</td></tr>
      <tr><td><code>.c--slider-a__item--clone</code></td><td>Item</td><td>DOM clone added by the loop manager</td></tr>
      <tr><td><code>.c--slider-a--initialized</code></td><td>Container</td><td>Added once JS layout is complete</td></tr>
      <tr><td><code>.c--slider-a--frozen</code></td><td>Container</td><td>All items fit — interaction disabled</td></tr>
      <tr><td><code>.c--slider-a--disabled</code></td><td>Container</td><td>Manually disabled via <code>disable()</code></td></tr>
      <tr><td><code>.c--slider-a--vertical</code></td><td>Container</td><td>Vertical direction mode</td></tr>
    </tbody>
  </table>

  <h2>Minimum required CSS</h2>
  <pre><code>{`.c--slider-a {
  overflow: hidden;
  position: relative;
}

.c--slider-a__wrapper {
  display: flex;
  will-change: transform;
}

.c--slider-a__item {
  flex-shrink: 0;
}`}</code></pre>

  <h2>Effect plugin classes</h2>
  <p>When an effect plugin is active, a modifier class is added to the container:</p>
  <table>
    <thead><tr><th>Plugin</th><th>Container class</th></tr></thead>
    <tbody>
      <tr><td>fadeEffect</td><td><code>.c--slider-effect-fade</code></td></tr>
      <tr><td>flipEffect</td><td><code>.c--slider-effect-flip</code></td></tr>
      <tr><td>cardsEffect</td><td><code>.c--slider-effect-cards</code></td></tr>
      <tr><td>creativeEffect</td><td><code>.c--slider-effect-creative</code></td></tr>
    </tbody>
  </table>
  <p>Use these to scope slide transition CSS:</p>
  <pre><code>{`.c--slider-effect-fade .c--slider-a__item {
  transition: opacity 400ms ease;
}`}</code></pre>

  <h2>Custom properties</h2>
  <p>You can use CSS custom properties to make the slider theme-aware:</p>
  <pre><code>{`.c--slider-a {
  --slider-height: 400px;
  height: var(--slider-height);
}`}</code></pre>
</DocsLayout>
