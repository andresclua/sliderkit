{
  "name": "sliderkit-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "SliderKit monorepo — a modern, accessible slider library",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter='@andresclua/*' build",
    "build:core": "pnpm --filter @andresclua/sliderkit build",
    "build:plugins": "pnpm --filter @andresclua/sliderkit-plugins build",
    "build:effects": "pnpm --filter @andresclua/sliderkit-effects build",
    "build:webgl": "pnpm --filter @andresclua/sliderkit-webgl build",
    "dev": "pnpm --filter sliderkit-playground dev",
    "docs": "pnpm --filter sliderkit-docs dev",
    "docs:build": "pnpm --filter sliderkit-docs build",
    "lint": "eslint packages/*/src --ext .ts",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "size-check": "pnpm -r size-check"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "vite": "^5.2.12"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
